import sys
import os
import requests

# Assuming the server is running on localhost:7777 or we can simulate it
# Let's just write a test script to query the database using SQLAlchemy to see if the relation is there
# Wait, let's just use FastAPI testing via TestClient or simply query the DB.

print("Test script created")
